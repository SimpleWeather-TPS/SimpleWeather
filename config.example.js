// Example config for local development. Copy to config.js via the generator script.
// Do not commit your real API key; use .env.local and npm run generate:config instead.
export const API_KEY = 'YOUR_API_KEY_HERE';
